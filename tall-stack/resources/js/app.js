import './bootstrap';

// NOTE: Alpine.js is bundled with Livewire 3 - do NOT import it manually!
// Livewire automatically starts Alpine with the correct timing.
//
// If you need Alpine plugins, register them via Livewire's Alpine instance:
// document.addEventListener('livewire:init', () => {
//     Alpine.plugin(somePlugin);
// });
